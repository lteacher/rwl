<UserControl x:Class="RobotInitial.View.BrickTabView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
			 xmlns:vm="clr-namespace:RobotInitial.ViewModel"
			 xmlns:vw="clr-namespace:RobotInitial.View"
			 xmlns:local="clr-namespace:RobotInitial"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="300">
	<UserControl.Resources>
        <ResourceDictionary Source="..\MainWindowResources.xaml"/>
    </UserControl.Resources>
	
    <ScrollViewer ScrollViewer.VerticalScrollBarVisibility="Auto">
    	<StackPanel Height="Auto" Width="Auto">
    		<vw:BlockView Style="{StaticResource MoveStyle}" Margin="0,40,0,0" BlockFill="Green" />
            <vw:BlockView Style="{StaticResource LoopStyle}" Margin="0,40,0,0" BlockFill="Red" />
            <vw:BlockView Style="{StaticResource SwitchStyle}" Margin="0,40,0,0" BlockFill="Yellow" />
            <vw:BlockView Style="{StaticResource WaitStyle}" Margin="0,40,0,0" BlockFill="Blue" />
		</StackPanel>
        <!--<ItemsControl ItemsSource="{Binding Path=Bricks}" /> -->
    </ScrollViewer>
</UserControl>
